#include <stdio.h>
#define PI 3.14

int main()
{
    // int a = 8;
    // float b = 7.333;
    // printf("Hello World\n");
    // printf("the value of a is %d and the value of b is %5.9f\n", a, b);
    // printf("the value of a is %d and the value of b is %11.5f\n", a, b);
    // printf("the value of a is %d and the value of b is %0.5f\n", a, b);
    // printf("the value of a is %d and the value of b is %0.9f\n", a, b);


    int a=15;
    const float b=27.999;
    // b= 27.222; // cannot do this since b is a constant 
    printf("%f", PI);
    // PI = 7.33; // cannot do this since pi is a constant

    return 0;
}
